import {z} from 'zod';import {operations} from './types';
export const GatewayRequestSchema=z.object({operation:z.enum(operations),input:z.unknown(),preferredProvider:z.string().optional(),preferredModel:z.string().optional(),requiredCapabilities:z.array(z.string()).optional(),dataClassification:z.enum(['public','internal','confidential','restricted']).default('internal'),jurisdiction:z.string().default('US'),maxCostUsd:z.number().nonnegative().optional(),maxLatencyMs:z.number().positive().optional(),allowCloud:z.boolean().default(true),allowLocal:z.boolean().default(true),allowFallback:z.boolean().default(true),requireHumanApproval:z.boolean().optional(),metadata:z.record(z.string(),z.unknown()).optional()}).strict();
export const StressRequestSchema=z.object({scenario:z.string()});
