import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.redledgerprotocol',
  appName: 'redledger protocol',
  webDir: 'dist'
};

export default config;
