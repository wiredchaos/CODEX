# INBOX_UPLOADS

Upload Vercel ZIP drops here using GitHub's "Add file → Upload files" flow. The automation watches this folder and the repo root for new `.zip` files, then files them into `VERCEL_HISTORY/` and updates the manifest.
