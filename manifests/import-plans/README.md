# Import plans

Import plans declare the approved target, source repository, and runtime relationship that deployment receipts must match. They are governance metadata only; they do not deploy or mutate production resources.
